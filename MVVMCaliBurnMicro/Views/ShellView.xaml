<Window x:Class="MVVMCaliBurnMicro.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMCaliBurnMicro.Views"
        mc:Ignorable="d" Background="BlanchedAlmond" FontSize="16"
        Title="ShellView" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Path=fullName, Mode=OneWay}"  Grid.Row="1" Grid.Column="1" Background="White"></TextBlock>
        <TextBox Grid.Column="1" Grid.Row="2" x:Name="firstName" MinWidth="100" Margin="20"></TextBox>
        <TextBox Grid.Column="2" Grid.Row="2" x:Name="lastName" MinWidth="100" Margin="20"></TextBox>

        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="people"
                  SelectedItem="{Binding Path=selectedPerson, Mode=OneWayToSource}" 
                  DisplayMemberPath="firstName">

        </ComboBox>

        <TextBlock Grid.Row="3" Grid.Column="2" x:Name="selectedPerson_lastName" Background="White"></TextBlock>
        
        <Button Grid.Row="4" Grid.Column="1" x:Name="ClearText">Clear Names</Button>

        <Button x:Name="LoadPageOne" Grid.Column="1" Grid.Row="5">Load Page One</Button>

        <Button x:Name="LoadPageTwo" Grid.Column="2" Grid.Row="5">Load Page Two</Button>

        <ContentControl Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="5" x:Name="ActiveItem"/>
    </Grid>
</Window>
